import React from 'react';
import { Typography } from '@mui/material';

const CollectionView = () => {
  return <Typography>Collection view will go here</Typography>;
};

export default CollectionView;